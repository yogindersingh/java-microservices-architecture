{{- if .Values.accounts.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-accounts
  labels:
    {{- include "java-microservices.labels" . | nindent 4 }}
    app.kubernetes.io/name: accounts
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: accounts
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: accounts
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: accounts
          image: "{{ .Values.accounts.image.repository }}:{{ .Values.accounts.image.tag }}"
          imagePullPolicy: {{ .Values.accounts.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            {{- include "java-microservices.configServerEnv" . | nindent 12 }}
            - name: SPRING_DATASOURCE_URL
              value: {{ .Values.accounts.env.datasourceUrl | quote }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 90
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 5
          resources:
            {{- toYaml .Values.accounts.resources | nindent 12 }}
{{- end }}
